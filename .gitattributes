# Force LF + UTF-8 for all text files. The plugin sources are mirrored from a
# Windows SVN working copy; this prevents Windows checkouts from normalising
# to CRLF and flags any non-UTF-8 working-tree files as a clear divergence.
* text=auto eol=lf working-tree-encoding=UTF-8
